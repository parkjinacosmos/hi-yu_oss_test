Hi-yu!

\- Design -
Student No: 22412052

Name: 박진아
![alt text](image.png)

E-mail: [jiiinap31@yu.ac.kr](mailto:jiiinap31@yu.ac.kr)

github 주소: <https://github.com/parkjinacosmos>

\[ Revision history \]

| **Revision date** | **Version #** | **Description**         | **Author** |
| ----------------- | ------------- | ----------------------- | ---------- |
| 06/01/2026        | 0.01          | design. 다이어그램 구현 | 박진아     |
|                   |               |                         |            |
|                   |               |                         |            |
|                   |               |                         |            |
|                   |               |                         |            |
|                   |               |                         |            |

\= Contents =

- Introduction ..........................................................................................

2\. Class diagram ........................................................................................

3\. Sequence diagram ..................................................................................

4\. State machine diagram ............................................................................

5\. Implementation requirements ...................................................................

6\. Glossary ....................................................................................................

7\. References .................................................................................................

- **Introduction**

  1.1 Summary

본 문서는 영남대학교 전용 맛집 정보 공유 플랫폼 'Hi-yu!' 시스템 개발을 위한 디자인 문서(Design Document)로, 이전 단계(Conceptualization 및 Analysis)에서 도출된 소프트웨어 요구사항을 바탕으로 객체지향 아키텍처와 구체적인 정적/동적 설계 구조를 명세한다. 본 문서의 전반부에는 시스템을 구성하는 객체들과 그 관계를 정의한 클래스 다이어그램(Class Diagram)을 제시하고, 후반부에는 주요 유스케이스(Use Case)에 따른 객체 간의 상호작용을 시간 순서로 기술한 시퀀스 다이어그램(Sequence Diagram) 및 화면/시스템의 상태 전이를 나타낸 상태 머신 다이어그램(State Machine Diagram)을 포함한다. 최종적으로는 이를 구현하기 위한 하드웨어/소프트웨어 요구사항과 용어 정의를 기술하여 실제 구현 단계의 지침서 역할을 하도록 구성하였다.

본 프로젝트인 'Hi-yu!'는 영남대학교 캠퍼스 주변 상권이 매우 넓고 식당이 방대하여 학생들이 광고성 리뷰가 아닌 신뢰할 수 있는 맛집 정보를 얻기 어렵고, 기존 포털 및 익명 커뮤니티가 대학생들의 실제 입맛과 예산(가격대)을 반영하지 못하는 한계점을 해결하기 위해 기획된 웹/앱 기반 상권 정보 공유 플랫폼이다. 본 시스템은 영남대학교 재학생(Student) 및 인근 식당 점주(Owner)를 주요 타겟 사용자로 하며, 학교 웹메일 연동 기반의 철저한 소속 인증(Verification), 실시간 지도 기반 구역별 탐색(Move), 유해어 필터링(Is Danger)이 적용된 신뢰성 높은 리뷰 시스템 등의 차별화된 기능을 제공함으로써 학우들에게 검증된 정보를 제공하고 지역 상권과의 상생을 도모하는 것을 최종 목적으로 한다.

1.2 Important Points of Design

본 시스템을 설계하는 과정에서 시스템의 안정성, 확장성, 그리고 유지보수 용이성을 확보하기 위해 가장 중점적으로 고려한 디자인 핵심 사항은 다음과 같다.

객체지향 설계 원칙 및 정보 은닉 (Encapsulation & Information Hiding): 클래스 간의 결합도(Coupling)를 낮추고 응집도(Cohesion)를 높이기 위해 책임과 역할을 명확히 분리하였다. 특히 데이터의 무결성을 보장하기 위해 멤버 변수의 직접적인 외부 접근을 제한하고, 접근 제어자(Private)와 Getter/Setter 메소드를 통한 철저한 정보 은닉을 설계에 반영하였다. 또한, 식당(Restaurant)과 메뉴(Menu) 도메인 간에 강한 합성(Composition) 관계를 설정하여 데이터 생명 주기를 일관되게 관리하도록 설계하였다.

실시간 통신 및 확장성 (Real-time Communication & Scalability): 점주가 메뉴 상태를 변경하거나 사용자가 새 리뷰를 등록했을 때 새로고침 없이 즉각 피드백을 받을 수 있도록 WebSocket 및 SSE(Server-Sent Events) 기술 기반의 실시간 알림 시스템(Reception Message)을 설계에 도입하였다. 점심/저녁 시간대의 트래픽 집중과 대용량 지도 마커 로딩(2초 이내 처리)을 고려하여 동시성 제어 및 가용성을 극대화하는 데 중점을 두었다.

견고한 데이터 검증 및 예외 처리 (Data Verification & Exception Handling): 사용자가 리뷰를 등록할 때 서버단에서 악성 언어를 자동으로 판별하는 금칙어 필터 알고리즘(checkDanger)을 배치하여 시스템의 신뢰성을 전제했다. 또한, 잘못된 식당 정보나 폐업 정보가 있을 시 사용자가 즉각 제보할 수 있는 SOS 신고 기능(Send SOS)을 시퀀스 다이어그램에 구체화하여 데이터 무결성이 상시 유지되도록 대안 흐름을 설계하였다.

역할 기반 권한 분리 (Role-based Access Control): 시스템 사용자를 학생(Student), 점주(Owner), 관리자(Manager)의 3가지 액터(Actor)로 명확히 분리하여 User 상속 구조를 설계하였다. 학생은 탐색 및 리뷰 작성을, 점주는 상점 및 재고 관리(Update Goods)를, 관리자는 SOS 검토 및 데이터 강제 수정 권한을 가지도록 제어 프로세스를 철저히 구분하여 외부 접근 및 보안 권한 에러(HTTP 403)를 차단하도록 설계 포인트를 두었다.

**2\. Class diagram**

본 장에서는 'Hi-yu!' 시스템을 구성하는 정적 빌딩 블록인 클래스들의 구조와 이들 간의 관계를 정의한 클래스 다이어그램(Class Diagram)을 명세한다. 본 시스템은 객체지향 설계 원칙에 의거하여 데이터 은닉을 극복하고 응집도를 높이도록 구조화하였으며, 상속(Generalization), 합성(Composition), 연관(Association) 관계를 기반으로 설계하였다.

2.1 클래스 간 관계 및 다이어그램 구조 가이드

\- 일반화 관계 (Generalization / Inheritance): 최상위 추상 클래스인 User를 중심으로, 소속 인증을 거치는 Student, 상점 관리 주체인 Owner, 시스템 관제 주체인 Manager 클래스가 이를 상속받아 공통 속성과 인증 메소드를 공유하도록 설계한다.

\- 합성 관계 (Composition): Restaurant 클래스와 Menu 클래스는 강한 생명주기 종속성을 지닌 합성 관계로 바인딩된다. 식당 데이터가 파괴되거나 삭제될 경우, 해당 식당에 종속된 개별 음식 메뉴 데이터도 데이터베이스 무결성을 위해 자동으로 연쇄 소멸하도록 구조화한다.

\- 연관 및 의존 관계 (Association & Dependency): Student 클래스는 Review 및 SOSReport를 생성(Create)하는 연관 관계를 가지며, Review 컨트롤러는 객체 생성 전 유해성 여부를 판별하기 위해 내부에 필터 메소드를 포함한다.

2.2 클래스 세부 명세 (Detailed Class Specifications)

각 클래스를 구성하는 멤버 변수(Attributes)와 메소드(Methods)의 접근 제어자(Access Modifier: + Public, - Private) 및 데이터 타입 사양은 다음과 같다.

1. User (최상위 추상 클래스)

<div class="joplin-table-wrapper"><table><tbody><tr><th><p>구분</p></th><th><p>성격</p></th><th><p>식별자 및 데이터 타입</p></th><th><p>설명</p></th></tr><tr><td rowspan="3"><p>Attributes</p></td><td><p>Private</p></td><td><ol><li>userID: String</li></ol></td><td><p>사용자의 고유 시스템 식별자</p></td></tr><tr><td><p>Private</p></td><td><ol><li>email: string</li></ol></td><td><p>사용자 이메일 주소</p></td></tr><tr><td><p>Private</p></td><td><ol><li>password: String</li></ol></td><td><p>암호화되어 저장되는 비밀번호 데이터</p></td></tr><tr><td><p>Methods</p></td><td><p>Public</p></td><td><p>+ login(): boolean</p></td><td><p>입력된 정보로 세션을 인증하고 로그인을 수행하는 메소드</p></td></tr></tbody></table></div>

2. Student (User 상속 클래스)

<div class="joplin-table-wrapper"><table><tbody><tr><th><p>구분</p></th><th><p>성격</p></th><th><p>식별자 및 데이터 타입</p></th><th><p>설명</p></th></tr><tr><td rowspan="3"><p>Attributes</p></td><td><p>Private</p></td><td><ol><li>studentNo: String</li></ol></td><td><p>영남대학교 학번 ex)22412052</p></td></tr><tr><td><p>Private</p></td><td><ol><li>name: String</li></ol></td><td><p>학생 실명 정보</p></td></tr><tr><td><p>Private</p></td><td><ol><li>isVerified: boolean</li></ol></td><td><p>YU 웹메일 인증 통과 여부 상태 값.</p></td></tr><tr><td rowspan="4"><p>Methods</p></td><td><p>Public</p></td><td><p>+ verifyMail(code: String): boolean</p></td><td><p>발송된 6자리 코드를 대조하여 소속 인증을 완료함.</p></td></tr><tr><td><p>Public</p></td><td><p>+ searchRestaurant(area: String): List&lt;Restaurant&gt;</p></td><td><p>정문, 오렌지거리 등 구역 필터 기반 식당 탐색.</p></td></tr><tr><td><p>Public</p></td><td><p>+ writeReview(content: String, rating: int): void</p></td><td><p>특정 식당에 대해 평점 및 텍스트 리뷰를 작성</p></td></tr><tr><td><p>Public</p></td><td><p>+ sendSOS(reason: String): void</p></td><td><p>폐업 및 정보 오류 발견 시 관리자에게 수정 요청</p></td></tr></tbody></table></div>

3. Owner (User 상속 클래스)

| 구분       | 성격                                                 | 식별자 및 데이터 타입                                    | 설명                                             |
| ---------- | ---------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------ |
| Attributes | Private                                              | \- businessRegNo: String                                 | 사업자 등록 번호 (상점 소유권 검증용)            |
| Private    | \- managedRestaurantID: String                       | 해당 점주가 운영 및 관리하는 식당의 고유 식별자          |
| Methods    | Public                                               | \+ updateGoods(menuID: String, isSoldOut: boolean): void | 메뉴의 품절 상태를 실시간으로 전환 및 업데이트함 |
| Public     | \+ writeReply(reviewID: String, reply: String): void | 학생이 남긴 식당 리뷰에 대해 감사 및 피드백 답글 작성    |

4. Manager (User 상속 클래스)

| 구분       | 성격                                    | 식별자 및 데이터 타입                                 | 설명                                              |
| ---------- | --------------------------------------- | ----------------------------------------------------- | ------------------------------------------------- |
| Attributes | Private                                 | \- managerLevel: int                                  | 관리자 접근 권한 등급                             |
| Methods    | Public                                  | \+ reviewSOS(reportID: String): void                  | 학생들이 제보한 SOS 정보 수정 요청 내역을 검토함. |
| Public     | \+ forceUpdateData(resID: String): void | SOS 검토 후 식당의 정보를 강제로 수정하여 무결성 유지 |

5. Restaurant (식당 도메인 클래스)

| 구분       | 성격                          | 식별자 및 데이터 타입                                  | 설명                                                   |
| ---------- | ----------------------------- | ------------------------------------------------------ | ------------------------------------------------------ |
| Attributes | Private                       | \- restaurantID: String                                | 식당 고유 ID                                           |
| Private    | \- name: String               | 식당 명칭                                              |
| Private    | \- categoryArea: String       | 상권 위치 카테고리 (정문, 오렌지거리, 뒷문 등)         |
| Private    | \- address: String            | 식당의 실제 상세 지번/도로명 주소                      |
| Private    | \- averageRating: double      | 등록된 리뷰들의 평점을 합산 연산한 평균 별점 정보      |
| Private    | \- menuList: List&lt;Menu&gt; | 해당 식당이 보유한 음식 메뉴 인스턴스 배열 (합성 관계) |
| Methods    | Public                        | \+ updateAverageRating(newRating: int): void           | 새 리뷰가 등록되거나 삭제될 때 평균 평점을 자동 재계산 |

6. Menu (Restaurant 합성 클래스)

| 구분       | 성격                  | 식별자 및 데이터 타입                             | 설명                                                  |
| ---------- | --------------------- | ------------------------------------------------- | ----------------------------------------------------- |
| Attributes | Private               | \- menuID: String                                 | 메뉴 고유 일련번호                                    |
| Private    | \- menuName: String   | 음식 또는 상품의 명칭                             |
| Private    | \- price: int         | 해당 메뉴의 가격 데이터                           |
| Private    | \- isSoldOut: boolean | 해당 상품의 당일 품절 여부를 나타내는 상태 플래그 |
| Methods    | Public                | \+ setStatus(status: boolean): void               | 점주의 명령을 받아 품절 플래그 상태 값을 강제 토글함. |

7. Review (리뷰 엔티티 클래스)

| 구분       | 성격                    | 식별자 및 데이터 타입                    | 설명                                              |
| ---------- | ----------------------- | ---------------------------------------- | ------------------------------------------------- |
| Attributes | Private                 | \- reviewID: String                      | 리뷰 데이터 고유 ID                               |
| Private    | \- studentID: String    | 리뷰를 작성한 학생의 유저 ID 식별자      |
| Private    | \- restaurantID: String | 대상 식당의 고유 ID 식별자               |
| Private    | \- content: String      | 리뷰 텍스트 본문 내용                    |
| Private    | \- rating: int          | 학생이 부여한 만족도 별점 값 (1 ~ 5)     |
| Private    | \- timestamp: String    | 리뷰가 등록된 날짜 및 시간 기록          |
| Methods    | Public                  | \+ checkDanger(content: String): boolean | DB 저장 전 텍스트 내 유해어/욕설 포함 여부를 검사 |

8. SOSReport (신고/제보 엔티티 클래스)

| 구분       | 성격                    | 식별자 및 데이터 타입                               | 설명                                                   |
| ---------- | ----------------------- | --------------------------------------------------- | ------------------------------------------------------ |
| Attributes | Private                 | \- reportID: String                                 | SOS 제보 고유 번호                                     |
| Private    | \- studentID: String    | 제보를 접수한 학생의 ID 식별자                      |
| Private    | \- restaurantID: String | 정보 오류가 발생한 대상 식당의 ID                   |
| Private    | \- reason: String       | 신고 사유 카테고리 (예: 폐업, 위치 이전, 가격 상이) |
| Private    | \- timestamp: String    | 제보가 유입된 시점의 타임스탬프 데이터              |
| Methods    | Public                  | \+ processReport(): void                            | 관리자가 확인한 후 해당 제보 데이터 처리 상태를 변경함 |

**3\. Sequence diagram**

본 장에서는 'Hi-yu!' 시스템의 개념화(Conceptualization) 단계에서 도출된 핵심 유스케이스(Use Case) 시나리오들이 시스템 내부의 객체(Object) 간에 어떠한 메시지와 함수 호출을 통해 시계열적으로 상호작용하는지 기술한다. 각 시퀀스 다이어그램은 액터(Actor), 바운더리(View), 컨트롤러(Controller), 엔티티(Entity/Service) 객체 간의 데이터 흐름을 명세하며, 예외 상황에 대한 대안 흐름(Alternative Flow) 역시 포함하여 설계하였다.

3.1 로그인 및 YU 웹메일 소속 인증 (Login & Mail Verification)

\[상세 설명\]

위의 다이어그램은 사용자가 시스템에 접속하여 권한을 검증받는 로그인 및 YU 웹메일 인증(Verification) 과정을 표현하는 Sequence diagram이다. Student 액터가 LoginView 인터페이스를 통해 계정 정보를 입력하면, UserController는 해당 사용자의 소속 인증 여부를 엔티티 객체에서 조회한다.

만약 @yu.ac.kr 메일을 통한 최초 인증이 완료되지 않은 상태라면, alt 조건문에 의해 MailVerificationService가 기동되어 사용자의 메일로 6자리의 일회성 인증 코드를 발송한다. 사용자가 수신된 코드를 바운더리에 정확히 입력하여 대조 작업(verifyCode)을 성공적으로 통과하면, 엔티티의 인증 상태 값이 true로 업데이트되며 최종적으로 로그인 토큰이 발급되어 비기능적 요구사항인 1초 이내의 신속한 세션 연결이 완료된다.

3.2 지도 이동 및 구역별 식당 탐색 (Move & Explore Restaurant)

\[상세 설명\]

위의 그림은 사용자가 특정 구역의 식당 정보를 조회하기 위해 화면을 조작할 때 발생하는 지도 이동 및 식당 탐색(Move) 과정을 표현하는 Sequence diagram이다. 사용자가 정문, 오렌지거리, 뒷문 등의 구역 필터를 터치하거나 직접 지도를 드래그하면, MapView 바운더리는 현재 변경된 위경도 좌표 범위를 MapController로 전달한다.

MapController는 대용량 마커 데이터를 효율적으로 처리하기 위해 해당 반경 내에 존재하는 식당 데이터를 RestaurantService에 요청하고, 데이터베이스 내의 식당 이름, 별점 평점, 위치 좌표 등의 엔티티 속성 리스트를 인메모리 상으로 반환받는다. 최종적으로 가공된 식당 요약 정보 카드가 지도 화면 위에 마커와 함께 렌더링되며, 이 모든 탐색 과정은 사용자 편의성을 위해 2초 이내에 로딩되도록 구조화되었다.

3.3 리뷰 작성 및 유해어 사전 필터링 (Review Registration & Is Danger)

\[상세 설명\]

위의 다이어그램은 클린한 커뮤니티 신뢰도를 확보하기 위해 설계된 핵심 비즈니스 로직인 리뷰 작성 및 유해어 필터링(Is Danger) 과정을 표현하는 Sequence diagram이다. 학생 사용자가 텍스트 리뷰와 별점을 기입한 후 등록 버튼을 누르면, 데이터베이스에 바로 저장되지 않고 ReviewController 내의 checkDanger() 검증 알고리즘 프로세스를 거치게 된다.

이 단계에서 서버는 0.1초 이내에 욕설 및 비방글 사전을 대조하여, 유해 단어가 감지될 경우(isDanger == true) 트랜잭션을 즉각 중단하고 화면에 경고 팝업을 출력하여 악성 게시글의 등록을 원천 차단한다. 유해어가 없는 정상적인 리뷰로 판명될 경우에만 Review 엔티티 인스턴스가 생성되어 영속화되며, 상위 합성 관계에 있는 Restaurant 객체의 평균 별점 속성을 실시간으로 갱신 연산하도록 정교하게 설계되었다.

3.4 식당 정보 오류 제보 및 SOS 요청 (Information Correction Request)

\[상세 설명\]

위의 다이어그램은 식당 데이터의 최신성과 무결성을 영남대생 집단지성을 통해 유지하기 위한 정보 수정 요청(Send SOS) 과정을 표현하는 Sequence diagram이다. 사용자가 지도나 상세 페이지에서 실제 상권 정보와 불일치하는 내역(예: 폐업, 위치 이전, 잘못된 가격)을 발견하여 SOS 신고를 접수하면, SOSController는 시스템 내부에 독립적인 SOSReport 객체를 생성하여 신고 유형과 타겟 식당 ID를 저장한다.

생성된 리포트 정보는 소멸되거나 누락되지 않고 관리자(Manager)의 실시간 관제 대시보드로 즉시 라우팅되어 고지된다. 관리자는 이를 검토하여 식당 데이터를 강제로 수정할 수 있는 시스템 권한을 가지게 되며, 이를 통해 플랫폼 데이터의 정합성을 상시 유지할 수 있는 상호작용 흐름을 보장한다.

3.5 점주의 메뉴 실시간 재고 업데이트 (Menu Status Update)

\[상세 설명\]

위의 다이어그램은 식당 점주가 매장의 상황을 즉각적으로 학생 유저들에게 전파하기 위해 실행하는 메뉴 상태 업데이트(Update Goods) 과정을 표현하는 Sequence diagram이다. Owner 액터가 본인의 상점 관리 바운더리인 InventoryView에서 특정 음식의 품절 스위치를 조작하면, MenuController로 고유 메뉴 식별자와 바뀐 상태 값이 전달된다.=

식당(Restaurant)과 강한 합성 관계로 묶여 관리되는 하위 엔티티인 Menu 객체의 isSoldOut 속성 값이 'Available'에서 'SoldOut'으로 안전하게 변경 처리가 완료되면 컨트롤러는 점주 화면에 반영 완료 상태를 회신한다. 이 직후 내부적으로 상태 변경 이벤트가 발생하여 시스템에 접속 중인 전체 학생들의 화면에 실시간으로 품절 상태가 동기화되는 트리거 역할을 수행한다.

3.6 웹소켓 기반 실시간 메시지 및 알림 수신 (Real-time Notification)

\[상세 설명\]

위의 다이어그램은 사용자가 애플리케이션을 새로고침하지 않아도 점주의 답글이나 메뉴 품절 상태를 즉각 수신할 수 있도록 구현하는 실시간 알림 수신(Reception Message) 과정을 표현하는 Sequence diagram이다. 특정 식당의 정보 변경이나 리뷰 답글 등록 이벤트가 트리거되면, 백엔드의 NotificationServer가 작동을 시작한다.

서버는 SessionManager를 통해 알림을 받아야 하는 타겟 유저의 브라우저 세션 연결 여부를 실시간으로 파악한다. 사용자가 로그인하여 세션이 유지되고 있는 상태(alt)라면, 양방향 통신 기술인 WebSocket 또는 SSE 프로토콜 파이프라인을 타고 데이터가 클라이언트로 다이렉트 푸시된다. 결과적으로 비기능적 요구사항의 기준인 0.5초 이내의 지연 시간으로 유저 화면 우측 상단에 실시간 경고 배너와 미확인 알림 카운트 뱃지가 자동으로 리렌더링되는 유기적인 구조를 갖춘다.

**4\. State machine diagram**

본 장에서는 'Hi-yu!' 시스템 전체를 하나의 전역 객체로 간주하고, 시스템의 생명 주기(Life Time) 동안 외부 트리거 및 이벤트에 의해 변화하는 동적 행동 구조를 기술한 상태 머신 다이어그램(State Machine Diagram)을 명세한다. 본 시스템은 다수의 학우들과 점주가 실시간으로 상호작용하는 웹/앱 기반 플랫폼이므로, 사용자가 마주하는 화면 흐름을 통제하는 '클라이언트 시스템 상태'와 백엔드에서 인증 및 실시간 이벤트를 관리하는 '서버 시스템 상태'로 명확히 분리하여 설계하였다.

4.1 클라이언트 시스템 상태 머신 (Client-Side State Machine)

\[상세 설명\]

위의 다이어그램은 클라이언트 애플리케이션의 뷰(View) 컴포넌트 전환 관계를 상태 기반으로 표현한 State Machine Diagram이다. 프로그램이 실행되면 최우선적으로 SplashState에서 리소스 로딩을 마친 후 유저의 로그인 상태를 검증한다. 본 시스템의 핵심 비전인 소속 인증 강화를 위해, 학생 유저가 로그인을 하더라도 isVerified 속성이 false일 경우 즉각 MailVerifyingState로 전이되어 YU 웹메일 인증 절차를 거치도록 제약 조건을 두었다.

최종 인증을 통과해 메인 비즈니스 영역인 MainMapState에 진입하면 사용자는 지도를 이동하거나 구역을 탐색하며 대용량 마커 렌더링 상태를 반복하게 된다. 특정 마커를 선택하여 RestaurantDetailState로 이동하면 리뷰 작성 및 실시간 알림 수신이 활성화되며, 정보의 오류가 있을 때 SOSReportingState로 전이하여 관리자 대시보드로 즉시 데이터를 송신할 수 있도록 흐름을 구조화하였다. 만약 로그인한 계정의 역할이 점주일 경우, 일반 화면이 아닌 독립된 OwnerDashboardState로 진입해 메뉴 품절 스위치를 조작할 수 있는 전용 상태 변경 흐름을 보장한다.

4.2 서버 시스템 상태 머신 (Server-Side State Machine)

\[상세 설명\]

위의 다이어그램은 'Hi-yu!'의 백엔드 아키텍처 및 세션 컨트롤러가 외부 요청을 받아 트랜잭션을 처리하는 주기를 표현한 서버 측 State Machine Diagram이다. 서버는 구동과 동시에 기본적으로 ListeningState에 머무르며 클라이언트로부터 발생하는 네트워크 트래픽을 비동기식으로 대기한다.

보안 및 사용 권한 검증 요청이 들어오면 AuthenticatingState로 전이하여 암호화 패스워드 대조 및 토큰 발급을 1초 이내에 수행한다. 사용자가 리뷰 작성을 시도할 경우, 시스템 무결성을 위해 곧바로 데이터베이스에 저장하지 않고 TextFilteringState로 우선 전이하는 독창적인 흐름을 설계하였다. 금칙어 검사를 통과하지 못하면 예외 이벤트를 발생시키고 트랜잭션을 폐기한 뒤 즉시 ListeningState로 돌아가 서버 메모리 누수를 방지한다.

정상적인 리뷰 등록이나 점주의 품절 스위치 전환(updateGoods) 요청이 유입되어 QueryProcessingState에서 영속성 저장이 완결되면, 서버는 연쇄적으로 BroadcastingState로 진입한다. 이 상태에서 서버는 실시간 양방향 통신 모듈을 작동시켜 접속 중인 학생들의 활성화 세션으로 0.5초 이내에 동기화 패킷(receptionMessage)을 밀어내어(Push) 최종적인 비기능적 성능 요구사항을 완수하도록 정밀하게 설계하였다.

**5\. Implementation requirements**

본 장에서는 'Hi-yu!' 시스템을 원활하게 구축하고 실제 환경에서 구동하기 위해 필요한 하드웨어 및 소프트웨어 요구사항과 기타 운영 제약사항을 기술한다. 본 시스템은 다수의 학생과 점주가 동시 접속하여 지도 데이터를 로딩하고 실시간 알림을 주고받는 클라이언트-서버(Client-Server) 아키텍처를 기반으로 하므로, 이를 뒷받침할 수 있는 구동 사양이 요구된다.

5.1 Hardware Requirements (하드웨어 요구사항)

시스템은 서비스를 호스팅하는 서버 측과 사용자가 접속하는 클라이언트(모바일/PC) 측으로 나뉘며, 원활한 렌더링 및 트래픽 처리를 위해 아래와 같은 최소 사양을 권장한다.

1. 서버 (Server / DB)

CPU: Intel Xeon 또는 AMD EPYC 2 Cores 이상 (최소 500명 이상의 동시 접속 트래픽 및 실시간 알림 세션을 처리하기 위함)

RAM: 4GB 이상

Storage (HDD/SSD): 20GB 이상의 여유 공간 (유저 정보, 식당/메뉴 데이터 및 텍스트 리뷰 데이터 보관용)

Network: 1Gbps 이상의 광대역 네트워크 연결 필수 (WebSocket 및 SSE 통신 유지용)

2. 클라이언트 (Client - 모바일/PC 환경)

CPU: 쿼드코어 1.5GHz 이상의 모바일 프로세서 또는 Intel Pentium IV 이상

RAM: 2GB 이상 (지도 상의 대용량 마커 렌더링을 2초 이내에 처리하기 위한 최소 메모리)

Network: Wi-Fi 또는 4G/5G 모바일 네트워크 상시 연결 필수 (실시간 재고 상태 및 API 통신용)

5.2 Software Requirements (소프트웨어 요구사항)

1. 운영체제 (Operating System)

Server: Linux 기반 OS (Ubuntu 20.04 LTS 이상 권장) 또는 Windows Server

Client: Android 8.0 (Oreo) 이상, iOS 12.0 이상 또는 HTML5를 지원하는 모던 웹 브라우저(Chrome, Safari, Edge 등)

2. 구현 언어 및 프레임워크 (Implementation Language & Framework)

Backend: Java (JDK 11 이상) / Spring Boot 기반 (또는 Node.js), 도메인 객체지향 설계를 구현하고 실시간 세션을 관리하기 위한 웹 프레임워크

Frontend: Android SDK (Java/Kotlin) 또는 웹앱 구현을 위한 프론트엔드 프레임워크 (React, Vue 등)

Database: MySQL 또는 PostgreSQL (식당 도메인과 메뉴 합성 관계 데이터의 무결성을 유지하기 위한 RDBMS 채택)

3. 외부 API 및 라이브러리 연동 (External API)

Map API: Google Maps Platform API 연동 (식당 좌표 데이터 처리 및 화면 렌더링용)

Mail Protocol: SMTP 서버 라이브러리 연동 (영남대학교 웹메일 @yu.ac.kr 도메인으로 6자리 인증 코드를 발송하기 위함)

5.3 Non-functional Constraints (비기능적 운영 제약사항)

보안 프로토콜: 클라이언트 브라우저와 서버 간의 모든 데이터 통신(로그인, 리뷰 작성, SOS 제보)은 반드시 HTTPS 프로토콜을 적용하여 암호화해야 한다.

실시간 통신: 점주의 상태 업데이트(Update Goods)와 유저의 리뷰 알림(Reception Message)은 새로고침이 필요 없는 상태 머신 흐름을 구현하기 위해 WebSocket 또는 SSE(Server-Sent Events) 환경 하에서 구동되어야 한다.

**6\. Glossary**

| 용어 (Term)                | 설명 (Description)                                                                                                                                           |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Hi-yu! System              | 영남대학교 주변 상권의 식당 정보를 공유하고 관리하기 위해 본 프로젝트에서 기획 및 개발하는 타겟 웹/앱 시스템 명칭.                                           |
| Student (학생)             | 영남대 웹메일(@yu.ac.kr) 인증을 통과한 주 사용자(Actor). 지도 기반 식당 검색, 리뷰 작성 및 SOS 신고 권한을 가진다.                                           |
| Owner (점주)               | 영남대 인근 상권의 식당 운영자. 전용 대시보드를 통해 본인 매장의 메뉴 상태(품절 등)를 업데이트하고 리뷰에 답글을 달 수 있다.                                 |
| Manager (관리자)           | 시스템 전반의 무결성을 모니터링하는 주체. 학생들이 제보한 SOS 신고 내역을 검토하고 데이터를 강제 수정할 수 있는 최고 권한을 가진다.                          |
| SOS (정보 수정 요청)       | 사용자가 폐업, 메뉴 변경 등 실제 상권과 다른 잘못된 식당 정보를 발견했을 때 관리자에게 알리는 제보 기능.                                                     |
| Is Danger (금칙어 필터)    | 리뷰 작성 시 데이터베이스 저장 이전에 부적절한 욕설이나 비방글을 컨트롤러 단에서 자동으로 감지하여 차단하는 자체 검증 로직.                                  |
| Verification (소속 인증)   | 외부인의 무분별한 접근을 막기 위해 영남대학교 고유 웹메일로 6자리 코드를 발송하여 정당한 사용 권한을 부여하는 보안 절차.                                     |
| Orange Street (오렌지거리) | 영남대학교 정문 앞의 주요 상권 밀집 지역을 의미하며, 시스템 내 지도 필터링의 주요 위치 카테고리로 분류된다.                                                  |
| Class Diagram              | 객체지향형 시스템 설계에서, 시스템을 구성하는 클래스들의 속성(Attributes), 메소드(Methods) 및 이들 간의 정적 관계(상속, 합성 등)를 도식화한 설계 다이어그램. |
| Sequence Diagram           | 시스템의 동적 모델링으로, 특정 유스케이스(Use Case)를 실행하기 위해 객체들이 시간의 흐름에 따라 주고받는 메시지(함수 호출)의 순서를 나타낸 다이어그램.       |
| State Machine Diagram      | 시스템이나 클라이언트 화면의 생명 주기 동안 발생하는 상태(State) 변화와, 그 상태 전이(Transition)를 유발하는 이벤트 및 트리거를 명세한 다이어그램.           |
| WebSocket / SSE            | 클라이언트가 새로고침을 하지 않아도 서버로부터 점주의 답글이나 품절 알림을 실시간으로 푸시(Push) 받을 수 있게 해주는 양방향/단방향 실시간 통신 프로토콜.     |

**7\. References**

1\. Google Maps Platform Documentation, 지도 API 연동 및 좌표 데이터 처리(대용량 마커 렌더링)를 위한 기술 참조 문서.

2\. Spring Framework Documentation / Node.js API, 실시간 통신(WebSocket) 및 아키텍처 패턴 구현을 위한 공식 레퍼런스 가이드.

3/ 소프트웨어 공학 강의 자료, Structural Modeling (Class Diagram) 및 Behavioral Modeling (Sequence, State Machine Diagram) 설계 원칙 가이드 (영남대학교).